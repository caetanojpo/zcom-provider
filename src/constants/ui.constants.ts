// Fallback color rendered behind background images to handle transparent edges
export const DEFAULT_BACKGROUND_COLOR = '#000';

// Default glow color for partner/client image hover — matches brand token zcom-200 (cyan)
export const DEFAULT_GLOW_COLOR = '#00FFFF';

// Square pixel dimensions for specific image contexts
export const CLIENT_IMAGE_SIZE = 100;
export const NAVBAR_LOGO_SIZE = 100;
export const NEWSLETTER_LOGO_SIZE = 100;
export const BENEFIT_IMAGE_SIZE = 90;
export const APP_STORE_BADGE_SIZE = 160;
export const HERO_LOGO_SIZE = 400;

// Full image quality — used for brand-critical assets where compression is unacceptable
export const IMAGE_QUALITY_FULL = 100;

// Default percentage of black overlay applied over background images
export const DARK_OVERLAY_DEFAULT_PERCENTAGE = 30;

// Embla carousel autoplay interval in milliseconds
export const CAROUSEL_AUTOPLAY_DELAY_MS = 2000;

// HTML anchor target values
export const LINK_TARGET_BLANK = '_blank';
export const LINK_TARGET_SELF = '_self';

// HTML lang attribute for the root document
export const HTML_LANG = 'pt-br';

// Next.js Script element ID for the organization JSON-LD block
export const SCHEMA_JSON_LD_SCRIPT_ID = 'zcom-schema';
